# =============================================================================
# Order Service — PR Preview Environment
# =============================================================================
name: "Backend: PR Preview Order Service"

on:
  pull_request:
    types: [labeled]
    paths:
      - "web-app/ecommerce-microservices/services/order-service/**"

jobs:
  preview:
    uses: ./.github/workflows/_reusable-preview.yml
    with:
      service-name: order-service
      service-path: web-app/ecommerce-microservices/services/order-service
      docker-image-name: webapp-order-service
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
      GITOPS_TOKEN: ${{ secrets.GITOPS_TOKEN }}
