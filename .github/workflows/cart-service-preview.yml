# =============================================================================
# Cart Service — PR Preview Environment
# =============================================================================
name: "Backend: PR Preview Cart Service"

on:
  pull_request:
    types: [labeled]
    paths:
      - "web-app/ecommerce-microservices/services/cart-service/**"

jobs:
  preview:
    uses: ./.github/workflows/_reusable-preview.yml
    with:
      service-name: cart-service
      service-path: web-app/ecommerce-microservices/services/cart-service
      docker-image-name: webapp-cart-service
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
      GITOPS_TOKEN: ${{ secrets.GITOPS_TOKEN }}
